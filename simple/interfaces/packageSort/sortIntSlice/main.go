package main

import (
	"fmt"
)

func main() {

	n := []int{1, 4, 65, 8, 98, 34, 2, 1}

	fmt.Println(n)

	//sort.Sort(sort.IntSlice(n))
	//sort.IntSlice(n).Sort()
	// Reverse
	//sort.Sort(sort.Reverse(sort.IntSlice(n)))
	fmt.Println(n)
}
